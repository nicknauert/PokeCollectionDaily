<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>PokeDex</title>
    <link rel="stylesheet" href="../styles.css">
  </head>
  <body>
    {{>_nav}}
    <main>
      <div class="results">
        <ul class="pokeUl">
          {{#poke}}
            <li class="pokeIndxItm">
              <a href="/id/{{Number}}"><img src="../img/{{Number}}.png" alt="thmb" class="indxImg"></a>
              <div class="indxHead">
                <p class="pokeNum">{{Number}}</p><h3 class="indxName"><a href="/id/{{Number}}"> {{Name}}</a></h3>
              </div>
              <p class="pokeType">Type: {{#Types}}<a href="/type/{{.}}">{{.}}</a> {{/Types}}</p>
            </li>
          {{/poke}}
          {{^poke}}
            <p>No Pokemon yet? Better get to training.</p>
          {{/poke}}
        </ul>
      </div>
    </main>
  </body>
</html>
